// Firebase configuration template.
// Copy this file to firebase-config.ts and fill in your project's values.
// firebase-config.ts is gitignored and should never be committed.
// Find these values in: Firebase Console → Project Settings → Your apps → SDK setup

export const firebaseConfig = {
    apiKey: "YOUR_WEB_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    databaseURL: "YOUR_FIREBASE_RTDB_URL",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID",
    measurementId: "YOUR_MEASUREMENT_ID"  // optional, only if using Analytics
};